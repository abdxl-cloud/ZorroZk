[package]
name = "script"
version = "0.1.0"
edition = "2024"

[dependencies]
sp1-sdk = "4.0"
sp1-verifier = "4.0"
hex = "0.4"
serde = { version = "1.0", features = ["derive"] }
shared = { path = "../shared" }
bincode = "1.3.1"

[build-dependencies]
sp1-build = "4.0"

